name: Release BigWigs

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag name for the release (e.g. v2.0.0)'
        required: true
        default: 'v2.0.0'
        type: string
      draft:
        description: 'Publish as draft release'
        required: false
        default: false
        type: boolean
      prerelease:
        description: 'Publish as pre-release'
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  build-and-release:
    name: Package and Release Addon
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Determine Version Tag
        id: vars
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ inputs.tag_name }}"
          else
            TAG="${{ github.ref_name }}"
          fi
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version_clean=$(echo ${TAG} | sed 's/^v//')" >> "$GITHUB_OUTPUT"

      - name: Package BigWigs Distribution Zip
        run: |
          mkdir -p dist
          python tools/package_release.py --version "${{ steps.vars.outputs.version_clean }}" --output-dir dist

      - name: Collect Release Assets
        id: assets
        run: |
          ASSETS="dist/*.zip"
          if [ -f "BigWigs_RaidLeader_Briefing.pdf" ]; then
            ASSETS="${ASSETS}"$'\n'"BigWigs_RaidLeader_Briefing.pdf"
          fi
          if [ -f "BigWigs_30141_Report.pdf" ]; then
            ASSETS="${ASSETS}"$'\n'"BigWigs_30141_Report.pdf"
          fi
          {
            echo "files<<EOF"
            echo "${ASSETS}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.vars.outputs.tag }}
          name: BigWigs ${{ steps.vars.outputs.tag }}
          draft: ${{ inputs.draft || false }}
          prerelease: ${{ inputs.prerelease || false }}
          generate_release_notes: true
          files: ${{ steps.assets.outputs.files }}
